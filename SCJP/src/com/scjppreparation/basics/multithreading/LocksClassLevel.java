package com.scjppreparation.basics.multithreading;

public class LocksClassLevel {
	
	
	//Every class in java has a unique lock
	
	//if a thread wants to execute a static synchronized mwthod then it requires a class level lock
	
	//while a thread executing a static synchronized method then the remaining threads are not allowed to execute any static
	
	//synchronized methods of that class simultaneously but the remaining threads are allowed to execute
	
	//Normal Static Methods
	
	//Normal Instacn methods
	
	//Synchronized instance methods
	
	//there is lo link between class level lock adn obejct level lock
	
	//both are independent of each other
	
	

}
