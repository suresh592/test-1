package com.scjppreparation.basics.modifiers;

class Default {
	
 	//if a class is declared as default tehn we can accesss that class within the same package
	//we cannot access it outside the package
	
	//if a  member is declared as public then we can access that member in this package only
	//outside of teh package we cannot access that
	
	//hence this menas default is also knoiwn as package level access
	
	void display()
	{
		System.out.println("This is in teh default class method");
	}
	public static void main(String[] args) {
		
		
		
	}

}
